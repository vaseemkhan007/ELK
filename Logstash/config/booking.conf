filter {
  if [fields][type] == "booking" {
    grok {
      match  => [ "message", "(%{DATA:browser} %{WORD:os} %{DATA:mobi_name} %{DATA:device} %{WORD:booking})|(%{DATA:browser} %{WORD:os} %{DATA:device} %{WORD:booking})"]
    }
    mutate {
      add_field => { "[@metadata][indexname]" => "booking" }
    }
  }
}
